def can_match(X, Y):
    for i in range(len(X)):
        if X[i] != Y[i] and X[i] != '?' and Y[i] != '?':
            return "No"

    return "Yes"


T = int(input())

for _ in range(T):
    X = input()
    Y = input()

    print(can_match(X, Y))
