def stringToNumber(s):
    num = 0
    for ch in s:
        digit = ord(ch) - ord("0")
        num = num * 10 + digit
    return num

if __name__ == '__main__':
    T = int(input())
    for i in range(T):
        s = input()
        print(stringToNumber(s))
