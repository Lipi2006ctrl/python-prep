T = int(input())

for _ in range(T):
    N = int(input())
    S = list(map(int, input().split()))

    S.sort()

    minimum = S[1] - S[0]

    for i in range(1, N - 1):
        difference = S[i + 1] - S[i]

        if difference < minimum:
            minimum = difference

    print(minimum)
