T = int(input())

for _ in range(T):
    N = int(input())
    A = list(map(int, input().split()))

    maximum = A[0]

    for i in range(1, N):
        if A[i] > maximum:
            maximum = A[i]

    print(maximum)
