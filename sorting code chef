T = int(input())

for _ in range(T):
    N, K = map(int, input().split())
    A = list(map(int, input().split()))

    A.sort()

    remaining = A[K:N-K]

    average = sum(remaining) / len(remaining)

    print(f"{average:.6f}")
